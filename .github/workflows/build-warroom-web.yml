name: Build warroom-web

on:
  push:
    branches:
      - main
    paths:
      - "02-apps/warroom-web/**"
      - ".github/workflows/build-warroom-web.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-warroom-web:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push warroom-web
        uses: docker/build-push-action@v6
        with:
          context: ./02-apps/warroom-web
          push: true
          tags: |
            ghcr.io/usomarriba22/con-warroom-web:dev
            ghcr.io/usomarriba22/con-warroom-web:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/usomarriba22/war
