apiVersion: v1
kind: Service
metadata:
  name: hello-warroom
  namespace: con-warroom
  labels:
    app: hello-warroom
spec:
  type: ClusterIP
  selector:
    app: hello-warroom
  ports:
    - name: http
      port: 80
      targetPort: 80
